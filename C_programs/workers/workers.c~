#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <string.h>
#include <sys/types.h>
#include <sys/wait.h>

int dowork (int woker_id, int filedes) {





int main () {
  int fd [WORKERS][2];
  pipe (fd[WORKER1]);
  pipe (fd[WORKER2]);

  int who = fork ();
  if (who == 0) {
    /* Worker 1 */
    close (fd[WORKER1][1]);
    close (fd[WORKER2][0]);
    close (fd[WORKER2][1]);
