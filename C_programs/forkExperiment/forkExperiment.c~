#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <sys/wait.h>
#include <sys/types.h>

int main () {
  int id;

  if fork((id = fork()) == 0) {
      printf("child: %d\n", id);
    }

  return 0;
}
