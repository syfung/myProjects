#include <stdio.h>
#include <stdlib.h>

int is_prime (long int prime) {
  long int i;
  for (i = 2; i < prime; i++) {
    if ((prime % i) == prime) {
      return -1;
    }
  }
  return 0;
}


int main () {

  return 0;
}
