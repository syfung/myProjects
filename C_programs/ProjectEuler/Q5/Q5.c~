# include <stdio.h>
# include <stdlib.h>
# include <math.h>

int main () {
  int num, diviser;

  for (num = 1; 1; num++) {
    for (diviser = 1; diviser <= 20; diviser++) {
      if (num % diviser != 0) {
	break;
      }
    }
  }
    
  printf ("-> %d\n", num);
  return 0;
}
